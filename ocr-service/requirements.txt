fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
easyocr>=1.7.2
pymupdf>=1.24
pillow>=10.4
numpy>=1.26
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7

# Crisis-document triage (classification, criticality scoring, translation),
# adapted from doc_triage_app on feature/ocr. scikit-learn trains a tiny
# in-memory model from hardcoded examples at import time — no external
# model download needed.
scikit-learn>=1.4.0
python-dateutil>=2.9.0
deep-translator>=1.11.4

# After `pip install -r requirements.txt`, also run once:
#   python -m spacy download en_core_web_sm
